<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/nc_black"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="70dp"
        android:paddingEnd="24dp"
        android:paddingBottom="32dp">

        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="37dp"
            android:layout_height="37dp"
            android:layout_marginBottom="20dp"
            android:background="@android:color/transparent"
            android:contentDescription="Go back"
            android:padding="0dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_back_arrow"
            android:tint="@color/nc_text_primary" />


        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Payment"
            android:textColor="@color/nc_text_secondary"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="14dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Submit Payment"
            style="@style/TextAppearance.NothingCinema.Display"
            android:layout_marginBottom="18dp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_card"
            android:padding="18dp"
            android:layout_marginBottom="18dp">

            <TextView
                android:id="@+id/bookingDetailsTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/TextAppearance.NothingCinema.BodySecondary"
                android:lineSpacingExtra="4dp" />
        </LinearLayout>

        <TextView
            android:id="@+id/selectedPaymentMethodTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Selected Payment: None"
            style="@style/TextAppearance.NothingCinema.Body"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/kpayButton"
            style="@style/Widget.NothingCinema.Button.Secondary"
            android:text="KPay"
            android:layout_marginBottom="10dp" />

        <Button
            android:id="@+id/wavePayButton"
            style="@style/Widget.NothingCinema.Button.Secondary"
            android:text="Wave Pay"
            android:layout_marginBottom="10dp" />

        <Button
            android:id="@+id/ayaPayButton"
            style="@style/Widget.NothingCinema.Button.Secondary"
            android:text="AYA Pay"
            android:layout_marginBottom="10dp" />

        <Button
            android:id="@+id/uabPayButton"
            style="@style/Widget.NothingCinema.Button.Secondary"
            android:text="UAB Pay"
            android:layout_marginBottom="16dp" />

        <ImageView
            android:id="@+id/receiptImageView"
            android:layout_width="match_parent"
            android:layout_height="220dp"
            android:layout_marginBottom="12dp"
            android:scaleType="centerCrop"
            android:contentDescription="Payment Receipt Preview"
            android:src="@drawable/ic_placeholder" />

        <Button
            android:id="@+id/chooseReceiptButton"
            style="@style/Widget.NothingCinema.Button.Secondary"
            android:text="Choose Payment Screenshot"
            android:layout_marginBottom="14dp" />

        <Button
            android:id="@+id/submitPaymentButton"
            style="@style/Widget.NothingCinema.Button.Primary"
            android:text="Submit Buy Payment" />

    </LinearLayout>
</ScrollView>